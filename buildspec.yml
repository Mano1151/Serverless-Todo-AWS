version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - echo "Using Node.js runtime"
  build:
    commands:
      - echo "Build started"
      - cd Backend
      - echo "No dependencies to install"
      - echo "Build completed successfully"

artifacts:
  files:
    - '**/*'
